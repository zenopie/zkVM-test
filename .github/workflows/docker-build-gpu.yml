name: Build and Push GPU Pre-compiled Image

on:
  push:
    branches:
      - main
    tags: "v[0-9]+.[0-9]+.[0-9]+"
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Set lowercase repository name
        id: set_repo
        run: echo "REPO_NAME=$(echo ${{ github.repository }} | tr '[:upper:]' '[:lower:]')" >> $GITHUB_OUTPUT

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push GPU Pre-compiled image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.gpu
          push: true
          tags: |
            ghcr.io/${{ steps.set_repo.outputs.REPO_NAME }}:gpu
            ghcr.io/${{ steps.set_repo.outputs.REPO_NAME }}:gpu-v30
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Output Image URL
        run: |
          echo "GPU Pre-compiled Image pushed to: ghcr.io/${{ steps.set_repo.outputs.REPO_NAME }}:gpu"
          echo "This image has CUDA binaries pre-compiled (no runtime build needed)"
